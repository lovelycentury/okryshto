# Fonts

Drop `.woff2` files here (e.g. `okryshto-sans-regular.woff2`), then add a matching
`@font-face` block in `../styles/fonts.scss`. Consumers get the files via the
package's `./fonts/*` export — no separate font hosting/CDN needed.
