{
  "extends": "./tsconfig.json",
  "include": ["src/**/*.ts", "src/**/*.tsx", "env.d.ts"],
  // Stories and unit tests live next to their component but are workbench/test-only —
  // keep them out of the published bundle's type build.
  "exclude": [
    "src/**/*.stories.ts",
    "src/**/*.stories.tsx",
    "src/**/*.spec.ts",
    "src/**/*.spec.tsx"
  ]
}
